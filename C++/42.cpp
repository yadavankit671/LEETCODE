#include<bits/stdc++.h>
using namespace std;
class Solution {
public:
    int trap(vector<int>& height) {
        stack<int> st;
        int i=0;
        // avoid initail blank spaces ::
        while(height.at(i)>0 && i<height.size()) i++;
        
    }
};
int main(){

}